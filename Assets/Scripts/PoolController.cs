using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PoolController : MonoBehaviour //This script is only for testing the functionality of the Pool.
{
    /*
    [SerializeField]
    private KeyCode poolBind;

    private void Update()
    {
        if (Input.GetKeyDown(poolBind))
        {
            PoolManager.Instance().CreateObject("Knives");
        }
    }
    */
}
